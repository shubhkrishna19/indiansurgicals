---
import { business } from "../data/business";
import { getAllCategories, getAllFamilies, getCatalogueExports } from "../data/catalogue";

const currentPath = Astro.url.pathname;
const categories = getAllCategories();
const families = getAllFamilies();
const catalogueExports = getCatalogueExports();
const contactSummary = `Call ${business.contact.phones[0]} | Email ${business.contact.email}`;
const categoryNameBySlug = new Map(categories.map((category) => [category.slug, category.name]));

const catalogueMenu = [
  {
    href: "/products",
    label: "Full product catalogue",
    note: `${categories.length} major product categories`,
  },
  ...categories.map((category) => ({
    href: `/products/${category.slug}`,
    label: category.name,
    note: `${category.familyCount} product families`,
  })),
  {
    href: catalogueExports.workbookPath,
    label: "Download official catalogue PDF",
    note: "Direct download of the approved company catalogue",
    download: true,
  },
];

const aboutMenu = [
  {
    href: "/about",
    label: "About the manufacturer",
    note: `${business.experienceClaim} of manufacturing experience`,
  },
  {
    href: "/get-quote",
    label: "Get quotation support",
    note: "WhatsApp, phone, and email enquiry options",
  },
  {
    href: "/contact",
    label: "Contact and enquiry desk",
    note: "Published phone, WhatsApp, email, and enquiry details",
  },
  {
    href: catalogueExports.workbookPath,
    label: "Download official catalogue PDF",
    note: "Direct PDF download for offline reference",
    download: true,
  },
];

const siteSearchItems = [
  {
    href: "/",
    label: "Home",
    note: "Company overview and featured product range",
    type: "Page",
    keywords: ["home", "homepage", "overview", business.brand],
  },
  {
    href: "/products",
    label: "Full Product Catalogue",
    note: "Browse the complete product range",
    type: "Page",
    keywords: ["catalogue", "catalog", "products", "range", "all products"],
  },
  {
    href: "/about",
    label: "About Indian Surgical Industries",
    note: "Manufacturer profile and company overview",
    type: "Page",
    keywords: ["about", "company", "profile", "manufacturer"],
  },
  {
    href: "/contact",
    label: "Contact",
    note: "Direct contact details and enquiry options",
    type: "Page",
    keywords: ["contact", "email", "phone", "whatsapp"],
  },
  {
    href: "/get-quote",
    label: "Get Quote",
    note: "Quotation support for model selection and quantities",
    type: "Page",
    keywords: ["quote", "quotation", "enquiry", "pricing"],
  },
  ...categories.map((category) => ({
    href: `/products/${category.slug}`,
    label: category.name,
    note: `${category.familyCount} product families`,
    type: "Category",
    keywords: [category.name, category.summary, category.intro],
  })),
  ...families.map((family) => ({
    href: `/products/${family.categorySlug}/${family.slug}`,
    label: family.name,
    note: categoryNameBySlug.get(family.categorySlug) ?? "Product family",
    type: "Product",
    keywords: [
      family.name,
      family.summary,
      family.description,
      family.subheading,
      ...(family.highlights ?? []),
      ...(family.notes ?? []),
    ],
  })),
];
const siteSearchItemsJson = JSON.stringify(siteSearchItems);

function isActive(href: string) {
  if (href === "/") {
    return currentPath === "/";
  }
  return currentPath.startsWith(href);
}

const isCatalogueActive = currentPath.startsWith("/products");
const isAboutActive = currentPath.startsWith("/about");
---

<header class="site-header">
  <div class="site-header__notice">
    <div class="container site-header__notice-inner">
      <span>{business.experienceClaim} manufacturing legacy</span>
      <span>{contactSummary}</span>
    </div>
  </div>

  <div class="container site-header__bar">
    <a href="/" class="site-header__brand">
      <span class="site-header__mark">
        <img src="/life-equip-logo.png" alt={`${business.brandMark} logo`} />
      </span>
      <span class="site-header__brand-copy">
        <strong>{business.brand}</strong>
        <small>{business.tagline}</small>
      </span>
    </a>

    <div class="site-header__actions">
      <div class="site-header__search" data-site-search>
        <button
          class="site-header__search-toggle"
          type="button"
          aria-expanded="false"
          aria-controls="site-header-search-panel"
          data-site-search-toggle
        >
          <span class="site-header__search-icon" aria-hidden="true"></span>
          <span>Search</span>
        </button>

        <div
          class="site-header__search-panel"
          id="site-header-search-panel"
          hidden
          data-site-search-panel
        >
          <label class="site-header__search-field">
            <span class="site-header__sr-only">Search the website</span>
            <input
              type="search"
              placeholder="Search products, categories, pages"
              autocomplete="off"
              data-site-search-input
            />
          </label>
          <div class="site-header__search-results" data-site-search-results></div>
        </div>
      </div>

      <a href="/get-quote" class="site-header__cta">
        <span class="site-header__cta-kicker">Quick enquiry support</span>
        <strong>Get Quote</strong>
      </a>

      <button
        class="site-header__toggle"
        type="button"
        aria-expanded="false"
        aria-controls="primary-navigation"
        data-mobile-nav-toggle
      >
        <span class="site-header__toggle-lines" aria-hidden="true"></span>
        <span class="site-header__toggle-label">Menu</span>
      </button>
    </div>

    <nav class="site-header__nav" aria-label="Primary" id="primary-navigation" data-mobile-nav>
      <div class="site-header__mobile-search">
        <label class="site-header__search-field site-header__search-field--mobile">
          <span class="site-header__sr-only">Search the website</span>
          <input
            type="search"
            placeholder="Search products, categories, pages"
            autocomplete="off"
            data-mobile-site-search-input
          />
        </label>
        <div
          class="site-header__search-results site-header__search-results--mobile"
          data-mobile-site-search-results
          hidden
        ></div>
      </div>

      <a href="/" class:list={["site-header__link", { "is-active": isActive("/") }]}>
        Home
      </a>

      <details class:list={["site-header__menu", { "is-active": isCatalogueActive }]} data-nav-menu>
        <summary class="site-header__summary" data-nav-summary>
          Catalogue
          <span class="site-header__chevron" aria-hidden="true"></span>
        </summary>

        <div class="site-header__dropdown site-header__dropdown--catalogue">
          <div class="site-header__dropdown-grid site-header__dropdown-grid--catalogue">
            {catalogueMenu.map((item) => (
              <a href={item.href} class="site-header__dropdown-link" download={item.download ? true : undefined}>
                <strong>{item.label}</strong>
                <small>{item.note}</small>
              </a>
            ))}
          </div>
        </div>
      </details>

      <details class:list={["site-header__menu", { "is-active": isAboutActive }]} data-nav-menu>
        <summary class="site-header__summary" data-nav-summary>
          About
          <span class="site-header__chevron" aria-hidden="true"></span>
        </summary>

        <div class="site-header__dropdown">
          <div class="site-header__dropdown-grid">
            {aboutMenu.map((item) => (
              <a href={item.href} class="site-header__dropdown-link" download={item.download ? true : undefined}>
                <strong>{item.label}</strong>
                <small>{item.note}</small>
              </a>
            ))}
          </div>
        </div>
      </details>

      <a href="/contact" class:list={["site-header__link", { "is-active": isActive("/contact") }]}>
        Contact
      </a>

      <a href="/get-quote" class="site-header__cta site-header__cta--mobile">
        <span class="site-header__cta-kicker">Quick enquiry support</span>
        <strong>Get Quote</strong>
      </a>
    </nav>
  </div>
</header>

<script
  is:inline
  type="application/json"
  id="site-header-search-data"
  set:html={siteSearchItemsJson}
></script>

<script>
  type SearchItem = {
    href: string;
    label: string;
    note: string;
    type: string;
    keywords?: string[];
  };

  const desktopNav = window.matchMedia("(min-width: 921px)");
  const mobileNav = window.matchMedia("(max-width: 920px)");
  const header = document.querySelector(".site-header");
  const mobileNavElement = document.querySelector<HTMLElement>("[data-mobile-nav]");
  const mobileToggle = document.querySelector<HTMLButtonElement>("[data-mobile-nav-toggle]");
  const navLinks = Array.from(
    document.querySelectorAll<HTMLAnchorElement>(".site-header__nav a"),
  );
  const menus = Array.from(
    document.querySelectorAll<HTMLDetailsElement>("[data-nav-menu]"),
  );
  const closeTimers = new WeakMap<HTMLDetailsElement, number>();
  const searchRoot = document.querySelector<HTMLElement>("[data-site-search]");
  const searchToggle = document.querySelector<HTMLButtonElement>("[data-site-search-toggle]");
  const searchPanel = document.querySelector<HTMLElement>("[data-site-search-panel]");
  const searchInput = document.querySelector<HTMLInputElement>("[data-site-search-input]");
  const searchResults = document.querySelector<HTMLElement>("[data-site-search-results]");
  const mobileSearchInput = document.querySelector<HTMLInputElement>("[data-mobile-site-search-input]");
  const mobileSearchResults = document.querySelector<HTMLElement>("[data-mobile-site-search-results]");
  const searchDataNode = document.getElementById("site-header-search-data");
  const searchItems: SearchItem[] = searchDataNode?.textContent
    ? (JSON.parse(searchDataNode.textContent) as SearchItem[])
    : [];

  const setMobileMenuState = (open: boolean) => {
    if (!(mobileNavElement instanceof HTMLElement) || !(mobileToggle instanceof HTMLButtonElement)) {
      return;
    }

    mobileNavElement.classList.toggle("is-open", open);
    mobileToggle.setAttribute("aria-expanded", String(open));
    header?.classList.toggle("is-mobile-open", open);
    document.body.classList.toggle("mobile-nav-open", open);

    if (!open) {
      menus.forEach((menu) => menu.removeAttribute("open"));
      if (mobileSearchInput instanceof HTMLInputElement) {
        mobileSearchInput.value = "";
      }
      renderSearchResults(mobileSearchResults, "");
    }
  };

  const renderSearchResults = (target: HTMLElement | null, query = "") => {
    if (!(target instanceof HTMLElement)) {
      return;
    }

    const normalizedQuery = query.trim().toLowerCase();
    target.hidden = !normalizedQuery;

    if (!normalizedQuery) {
      target.replaceChildren();
      return;
    }

    const results = searchItems
      .filter((item: SearchItem) => {
        const haystack = [
          item.label,
          item.note,
          item.type,
          ...(Array.isArray(item.keywords) ? item.keywords : []),
        ]
          .join(" ")
          .toLowerCase();

        return haystack.includes(normalizedQuery);
      })
      .slice(0, normalizedQuery ? 8 : 6);

    target.replaceChildren();

    if (!results.length) {
      const empty = document.createElement("p");
      empty.className = "site-header__search-empty";
      empty.textContent = "No matching pages or products found.";
      target.appendChild(empty);
      return;
    }

    const list = document.createElement("div");
    list.className = "site-header__search-list";

    results.forEach((item: SearchItem) => {
      const link = document.createElement("a");
      link.href = item.href;
      link.className = "site-header__search-result";

      const type = document.createElement("small");
      type.textContent = item.type;

      const label = document.createElement("strong");
      label.textContent = item.label;

      const note = document.createElement("span");
      note.textContent = item.note;

      link.append(type, label, note);
      list.appendChild(link);
    });

    target.appendChild(list);
  };

  const setSearchOpen = (open: boolean) => {
    if (!(searchToggle instanceof HTMLButtonElement) || !(searchPanel instanceof HTMLElement)) {
      return;
    }

    searchToggle.setAttribute("aria-expanded", String(open));
    searchRoot?.classList.toggle("is-open", open);
    searchPanel.hidden = !open;

    if (open) {
      renderSearchResults(searchResults, searchInput?.value ?? "");
      window.requestAnimationFrame(() => searchInput?.focus());
    }
  };

  const closeMenus = (except?: HTMLDetailsElement | null) => {
    menus.forEach((menu) => {
      if (menu !== except) {
        const timer = closeTimers.get(menu);
        if (timer) {
          window.clearTimeout(timer);
        }
        menu.removeAttribute("open");
      }
    });
  };

  const cancelClose = (menu: HTMLDetailsElement) => {
    const timer = closeTimers.get(menu);
    if (timer) {
      window.clearTimeout(timer);
      closeTimers.delete(menu);
    }
  };

  const scheduleClose = (menu: HTMLDetailsElement) => {
    cancelClose(menu);
    closeTimers.set(
      menu,
      window.setTimeout(() => {
        menu.removeAttribute("open");
        closeTimers.delete(menu);
      }, 180),
    );
  };

  menus.forEach((menu) => {
    const summary = menu.querySelector("[data-nav-summary]");
    if (!summary) return;

    menu.addEventListener("pointerenter", () => {
      if (!desktopNav.matches) return;
      cancelClose(menu);
      closeMenus(menu);
      menu.setAttribute("open", "");
    });

    menu.addEventListener("pointerleave", () => {
      if (!desktopNav.matches) return;
      scheduleClose(menu);
    });

    menu.addEventListener("focusin", () => {
      if (!desktopNav.matches) return;
      cancelClose(menu);
      closeMenus(menu);
      menu.setAttribute("open", "");
    });

    menu.addEventListener("focusout", (event) => {
      if (!desktopNav.matches) return;
      const nextTarget = (event as FocusEvent).relatedTarget;
      if (nextTarget instanceof Node && menu.contains(nextTarget)) return;
      scheduleClose(menu);
    });

    summary.addEventListener("click", (event) => {
      if (!desktopNav.matches) return;
      event.preventDefault();
      cancelClose(menu);
      const willOpen = !menu.hasAttribute("open");
      closeMenus(menu);
      if (willOpen) {
        menu.setAttribute("open", "");
      } else {
        menu.removeAttribute("open");
      }
    });
  });

  mobileToggle?.addEventListener("click", () => {
    if (!mobileNav.matches || !(mobileNavElement instanceof HTMLElement)) {
      return;
    }

    setMobileMenuState(!mobileNavElement.classList.contains("is-open"));
  });

  navLinks.forEach((link) => {
    link.addEventListener("click", () => {
      if (mobileNav.matches) {
        setMobileMenuState(false);
      }
      setSearchOpen(false);
    });
  });

  searchToggle?.addEventListener("click", () => {
    const isExpanded = searchToggle.getAttribute("aria-expanded") === "true";
    if (!isExpanded) {
      setMobileMenuState(false);
    }
    setSearchOpen(!isExpanded);
  });

  searchInput?.addEventListener("input", () => {
    renderSearchResults(searchResults, searchInput.value);
  });

  mobileSearchInput?.addEventListener("input", () => {
    renderSearchResults(mobileSearchResults, mobileSearchInput.value);
  });

  document.addEventListener("click", (event) => {
    const target = event.target;
    if (!(target instanceof Node)) return;

    if (searchRoot && !searchRoot.contains(target)) {
      setSearchOpen(false);
    }

    if (mobileNav.matches) {
      if (header?.contains(target)) {
        return;
      }
      setMobileMenuState(false);
      return;
    }

    if (!desktopNav.matches) return;
    if (menus.some((menu) => menu.contains(target))) return;
    closeMenus();
  });

  desktopNav.addEventListener("change", (event) => {
    if (!event.matches) {
      closeMenus();
    }
  });

  mobileNav.addEventListener("change", (event) => {
    if (!event.matches) {
      setMobileMenuState(false);
    }
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      setSearchOpen(false);
      setMobileMenuState(false);
      closeMenus();
    }
  });

  renderSearchResults(searchResults, "");
  renderSearchResults(mobileSearchResults, "");
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    backdrop-filter: blur(16px);
    background: rgba(245, 248, 250, 0.92);
    border-bottom: 1px solid rgba(84, 96, 109, 0.14);
  }

  .site-header__notice {
    border-bottom: 1px solid rgba(84, 96, 109, 0.1);
    background: linear-gradient(90deg, rgba(11, 27, 43, 0.98), rgba(25, 77, 128, 0.92));
    color: rgba(255, 255, 255, 0.88);
    font-size: 0.78rem;
  }

  .site-header__notice-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-block: 0.55rem;
  }

  .site-header__bar {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: 1rem;
    min-height: 5.35rem;
    padding-block: 0.75rem;
  }

  .site-header__brand {
    grid-column: 1;
    display: flex;
    align-items: center;
    gap: 0.95rem;
    min-width: 0;
    max-width: 24rem;
    justify-self: start;
  }

  .site-header__mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3.55rem;
    height: 3.55rem;
    flex: 0 0 3.55rem;
    border-radius: 1rem;
    padding: 0.18rem;
    background: rgba(255, 255, 255, 0.96);
    box-shadow:
      0 12px 22px rgba(201, 111, 40, 0.16),
      inset 0 0 0 1px rgba(84, 96, 109, 0.08);
  }

  .site-header__mark img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
  }

  .site-header__brand-copy {
    display: grid;
    min-width: 0;
  }

  .site-header__brand-copy strong {
    font-size: 0.98rem;
    line-height: 1.1;
  }

  .site-header__brand-copy small {
    color: var(--color-text-muted);
    font-size: 0.76rem;
    line-height: 1.4;
    max-width: 19.5rem;
  }

  .site-header__nav {
    grid-column: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.05rem;
    white-space: nowrap;
    min-width: 0;
    justify-self: center;
  }

  .site-header__actions {
    grid-column: 3;
    display: flex;
    align-items: center;
    gap: 0.65rem;
    position: relative;
    justify-self: end;
  }

  .site-header__search {
    position: relative;
  }

  .site-header__mobile-search {
    display: none;
  }

  .site-header__search-toggle {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    border: 1px solid rgba(84, 96, 109, 0.16);
    border-radius: 999px;
    justify-content: center;
    width: 3rem;
    height: 3rem;
    padding: 0;
    background: rgba(255, 255, 255, 0.9);
    color: var(--color-text);
    box-shadow: 0 10px 22px rgba(11, 27, 43, 0.06);
    font-size: 0.82rem;
    font-weight: 700;
    transition:
      border-color 160ms ease,
      background 160ms ease,
      transform 160ms ease;
  }

  .site-header__search-toggle:hover,
  .site-header__search-toggle:focus-visible,
  .site-header__search.is-open .site-header__search-toggle {
    border-color: rgba(25, 77, 128, 0.22);
    background: rgba(255, 255, 255, 0.98);
    transform: translateY(-1px);
  }

  .site-header__search-icon {
    position: relative;
    width: 0.9rem;
    height: 0.9rem;
    border: 2px solid currentColor;
    border-radius: 50%;
    display: inline-block;
  }

  .site-header__search-icon::after {
    content: "";
    position: absolute;
    right: -0.3rem;
    bottom: -0.28rem;
    width: 0.45rem;
    height: 2px;
    border-radius: 999px;
    background: currentColor;
    transform: rotate(45deg);
    transform-origin: right center;
  }

  .site-header__search-panel {
    position: absolute;
    top: calc(100% + 0.65rem);
    right: 0;
    width: min(27rem, calc(100vw - 2rem));
    padding: 0.9rem;
    border: 1px solid rgba(84, 96, 109, 0.14);
    border-radius: 1.1rem;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.99), rgba(245, 248, 250, 0.98));
    box-shadow: 0 22px 42px rgba(11, 27, 43, 0.16);
    z-index: 8;
  }

  .site-header__search-field {
    display: block;
  }

  .site-header__search-toggle span:last-child {
    display: none;
  }

  .site-header__search-field input {
    width: 100%;
    border: 1px solid rgba(84, 96, 109, 0.16);
    border-radius: 0.95rem;
    padding: 0.92rem 1rem;
    background: rgba(255, 255, 255, 0.96);
    color: var(--color-text);
    font: inherit;
    outline: none;
  }

  .site-header__search-field input:focus {
    border-color: rgba(25, 77, 128, 0.28);
    box-shadow: 0 0 0 4px rgba(25, 77, 128, 0.08);
  }

  .site-header__search-results {
    margin-top: 0.75rem;
  }

  .site-header__search-results[hidden] {
    display: none;
  }

  .site-header__search-list {
    display: grid;
    gap: 0.45rem;
  }

  .site-header__search-result {
    display: grid;
    gap: 0.18rem;
    padding: 0.78rem 0.85rem;
    border-radius: 0.92rem;
    border: 1px solid transparent;
    background: rgba(25, 77, 128, 0.03);
    transition:
      border-color 160ms ease,
      background 160ms ease,
      transform 160ms ease;
  }

  .site-header__search-result:hover,
  .site-header__search-result:focus-visible {
    border-color: rgba(25, 77, 128, 0.16);
    background: rgba(25, 77, 128, 0.08);
    transform: translateY(-1px);
  }

  .site-header__search-result small {
    color: var(--color-primary);
    font-size: 0.67rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .site-header__search-result strong {
    font-size: 0.92rem;
    line-height: 1.3;
  }

  .site-header__search-result span,
  .site-header__search-empty {
    color: var(--color-text-muted);
    font-size: 0.78rem;
    line-height: 1.45;
  }

  .site-header__search-empty {
    margin: 0;
    padding: 0.55rem 0.2rem 0.1rem;
  }

  .site-header__sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .site-header__menu {
    position: relative;
    padding-bottom: 0.8rem;
    margin-bottom: -0.8rem;
  }

  .site-header__summary,
  .site-header__link {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--color-text-muted);
    font-size: 0.92rem;
    font-weight: 600;
  }

  @media (max-width: 1280px) {
    .site-header__brand-copy small {
      max-width: 15rem;
      font-size: 0.72rem;
    }

    .site-header__nav {
      gap: 0.88rem;
    }

    .site-header__summary,
    .site-header__link {
      font-size: 0.86rem;
    }
  }

  @media (max-width: 1100px) {
    .site-header__brand-copy strong {
      font-size: 0.92rem;
    }

    .site-header__brand-copy small {
      display: none;
    }

    .site-header__nav {
      gap: 0.72rem;
    }

    .site-header__summary,
    .site-header__link {
      font-size: 0.81rem;
    }

    .site-header__search-toggle {
      width: 2.85rem;
      height: 2.85rem;
    }

    .site-header__cta {
      padding-inline: 0.88rem;
    }

    .site-header__cta strong {
      font-size: 0.92rem;
    }
  }

  .site-header__summary {
    cursor: pointer;
    list-style: none;
  }

  .site-header__summary::-webkit-details-marker {
    display: none;
  }

  .site-header__summary::after,
  .site-header__link::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -0.55rem;
    height: 2px;
    border-radius: 999px;
    background: var(--color-primary);
    opacity: 0;
    transform: scaleX(0.5);
    transition:
      opacity 160ms ease,
      transform 160ms ease;
  }

  .site-header__chevron {
    width: 0.5rem;
    height: 0.5rem;
    border-right: 1.7px solid currentColor;
    border-bottom: 1.7px solid currentColor;
    transform: rotate(45deg) translateY(-1px);
    transition: transform 180ms ease;
  }

  .site-header__link:hover,
  .site-header__summary:hover,
  .site-header__link.is-active {
    color: var(--color-text);
  }

  .site-header__link:hover::after,
  .site-header__summary:hover::after,
  .site-header__link.is-active::after,
  .site-header__menu.is-active > .site-header__summary::after,
  .site-header__menu[open] > .site-header__summary::after {
    opacity: 1;
    transform: scaleX(1);
  }

  .site-header__menu.is-active > .site-header__summary,
  .site-header__menu[open] > .site-header__summary {
    color: var(--color-text);
  }

  .site-header__menu[open] > .site-header__summary .site-header__chevron,
  .site-header__menu:hover > .site-header__summary .site-header__chevron,
  .site-header__menu:focus-within > .site-header__summary .site-header__chevron {
    transform: rotate(225deg) translateY(1px);
  }

  .site-header__dropdown {
    position: absolute;
    top: calc(100% + 0.3rem);
    left: 50%;
    width: min(24rem, calc(100vw - 2rem));
    padding: 0.95rem;
    border: 1px solid rgba(84, 96, 109, 0.14);
    border-radius: 1.1rem;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.99), rgba(245, 248, 250, 0.98));
    box-shadow: 0 22px 42px rgba(11, 27, 43, 0.16);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translate(-50%, -0.35rem);
    transition:
      opacity 180ms ease,
      transform 180ms ease,
      visibility 180ms ease;
  }

  .site-header__dropdown::before {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    top: -0.85rem;
    height: 0.85rem;
  }

  .site-header__dropdown--catalogue {
    width: min(44rem, calc(100vw - 2rem));
  }

  .site-header__dropdown-grid {
    display: grid;
    gap: 0.6rem;
  }

  .site-header__dropdown-grid--catalogue {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .site-header__dropdown-link {
    display: grid;
    gap: 0.2rem;
    padding: 0.8rem 0.9rem;
    border-radius: 0.9rem;
    border: 1px solid transparent;
    background: rgba(25, 77, 128, 0.03);
    transition:
      border-color 160ms ease,
      background 160ms ease,
      transform 160ms ease;
  }

  .site-header__dropdown-link:hover,
  .site-header__dropdown-link:focus-visible {
    border-color: rgba(25, 77, 128, 0.16);
    background: rgba(25, 77, 128, 0.08);
    transform: translateY(-2px);
  }

  .site-header__dropdown-link strong {
    font-size: 0.88rem;
    line-height: 1.3;
  }

  .site-header__dropdown-link small {
    color: var(--color-text-muted);
    font-size: 0.76rem;
    line-height: 1.45;
  }

  .site-header__menu:hover .site-header__dropdown,
  .site-header__menu:focus-within .site-header__dropdown,
  .site-header__menu[open] .site-header__dropdown {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translate(-50%, 0);
  }

  .site-header__cta {
    display: grid;
    gap: 0.12rem;
    border-radius: 1.05rem;
    padding: 0.72rem 1rem 0.78rem;
    background:
      linear-gradient(135deg, rgba(25, 77, 128, 1), rgba(14, 52, 90, 0.96)),
      var(--color-primary);
    color: white;
    box-shadow:
      0 14px 24px rgba(25, 77, 128, 0.22),
      inset 0 0 0 1px rgba(255, 255, 255, 0.1);
    text-align: left;
  }

  .site-header__cta-kicker {
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }

  .site-header__cta strong {
    font-size: 1rem;
    line-height: 1;
  }

  .site-header__cta--mobile,
  .site-header__toggle {
    display: none;
  }

  .site-header__toggle {
    align-items: center;
    gap: 0.55rem;
    border: 1px solid rgba(84, 96, 109, 0.16);
    border-radius: 999px;
    padding: 0.76rem 0.92rem;
    background: rgba(255, 255, 255, 0.92);
    color: var(--color-text);
    box-shadow: 0 10px 22px rgba(11, 27, 43, 0.08);
  }

  .site-header__toggle-lines {
    position: relative;
    display: inline-block;
    width: 1rem;
    height: 0.72rem;
  }

  .site-header__toggle-lines::before,
  .site-header__toggle-lines::after,
  .site-header__toggle-lines {
    border-top: 2px solid currentColor;
    border-radius: 999px;
  }

  .site-header__toggle-lines::before,
  .site-header__toggle-lines::after {
    content: "";
    position: absolute;
    left: 0;
    width: 100%;
  }

  .site-header__toggle-lines::before {
    top: 0.25rem;
  }

  .site-header__toggle-lines::after {
    top: 0.5rem;
  }

  .site-header__toggle-label {
    font-size: 0.84rem;
    font-weight: 700;
  }

  @media (max-width: 920px) {
    .site-header__notice {
      display: none;
    }

    .site-header__bar {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      gap: 0.85rem;
      min-height: auto;
      padding-block: 0.72rem;
    }

    .site-header__nav {
      position: absolute;
      top: calc(100% + 0.55rem);
      left: 0;
      right: 0;
      transform: none;
      width: 100%;
      display: none;
      padding: 0.95rem;
      border: 1px solid rgba(84, 96, 109, 0.14);
      border-radius: 1.2rem;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(245, 248, 250, 0.97));
      box-shadow: 0 22px 42px rgba(11, 27, 43, 0.14);
      z-index: 6;
      max-height: calc(100vh - 6rem);
      overflow-y: auto;
      overscroll-behavior: contain;
      scrollbar-width: thin;
      order: 3;
      grid-template-columns: 1fr;
      gap: 0.7rem;
      grid-column: 1 / -1;
      justify-self: stretch;
    }

    .site-header__nav.is-open {
      display: grid;
    }

    .site-header__actions {
      gap: 0.55rem;
      grid-column: 2;
      margin-left: 0;
    }

    .site-header__brand-copy small {
      display: none;
    }

    .site-header__cta--mobile,
    .site-header__toggle {
      display: inline-flex;
    }

    .site-header__cta--mobile {
      width: 100%;
      justify-content: center;
      text-align: center;
      margin-top: 0.35rem;
    }

    .site-header__actions .site-header__cta,
    .site-header__actions .site-header__search {
      display: none;
    }

    .site-header__menu {
      min-width: 0;
      padding-bottom: 0;
      margin-bottom: 0;
    }

    .site-header__mobile-search {
      display: grid;
      gap: 0.7rem;
      margin-bottom: 0.15rem;
    }

    .site-header__search-field--mobile input {
      padding: 0.88rem 0.95rem;
      background: rgba(255, 255, 255, 0.94);
    }

    .site-header__search-results--mobile {
      margin-top: 0;
    }

    .site-header__summary,
    .site-header__link {
      justify-content: space-between;
      width: 100%;
      padding: 0.82rem 0.95rem;
      border: 1px solid rgba(84, 96, 109, 0.14);
      border-radius: 0.95rem;
      background: rgba(255, 255, 255, 0.9);
    }

    .site-header__summary::after,
    .site-header__link::after {
      display: none;
    }

    .site-header__link.is-active,
    .site-header__menu.is-active > .site-header__summary {
      border-color: rgba(25, 77, 128, 0.24);
      background: rgba(25, 77, 128, 0.08);
    }

    .site-header__dropdown,
    .site-header__dropdown--catalogue {
      position: static;
      width: 100%;
      margin-top: 0.55rem;
      padding: 0.75rem;
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
      transform: none;
      display: none;
      box-shadow: none;
    }

    .site-header__menu[open] .site-header__dropdown {
      display: block;
    }

    .site-header__dropdown-grid--catalogue {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .site-header__mark {
      width: 2.8rem;
      height: 2.8rem;
      flex-basis: 2.8rem;
      border-radius: 0.82rem;
    }

    .site-header__brand {
      gap: 0.72rem;
    }

    .site-header__brand-copy strong {
      font-size: 0.92rem;
    }

    .site-header__brand-copy small {
      font-size: 0.68rem;
    }

    .site-header__actions .site-header__cta {
      display: none;
    }

    .site-header__toggle {
      padding: 0.72rem 0.88rem;
    }

    .site-header__toggle-label {
      font-size: 0.78rem;
    }
  }
</style>
